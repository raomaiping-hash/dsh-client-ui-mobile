/**
 * Mobile UI optimizer — host half.
 * Pure UI plugin: empty apply so the Loader mounts the package; the browser
 * half ships via exports["./client"] (package.json dsh.client).
 */
function apply() {}

export { apply }
